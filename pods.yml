apiVersion: v1
kind: Pod
metadata:
  name: kubernates
  labels:
    app: kubernates
spec:
  containers:
    - name: kubernates
      image: relventech/kubernates
      ports:
        - containerPort: 8080
